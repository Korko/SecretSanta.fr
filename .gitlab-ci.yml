image: lorisleiva/laravel-docker:latest

.before_script_npm:
  before_script:
    - npm install

phpunit:
  extends: .before_script_composer
  stage: test
  script:
    - cp .env.example .env
    - php artisan key:generate
    - php artisan test
